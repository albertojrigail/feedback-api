{
  "name": "feedback-api",
  "version": "1.0.0",
  "description": "API for funneling feedback on NemoBot",
  "main": "server.js",
  "scripts": {
      "dev" : "nodemon server.js"
      "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alberto Rigail",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "mongodb": "^3.5.9"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
